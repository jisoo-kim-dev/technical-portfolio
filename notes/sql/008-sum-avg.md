# Day 08: SUM & AVG

## SUM()

Returns the total of numeric values.

```sql
SELECT SUM(Age)
FROM Customers;
```

## AVG()

Returns the average of numeric values.

```sql
SELECT AVG(Age)
FROM Customers;
```

Both ignore NULL values.

## Summary

- SUM(column): total
- AVG(column): average
